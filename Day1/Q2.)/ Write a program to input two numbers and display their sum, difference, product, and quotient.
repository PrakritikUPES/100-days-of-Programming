num1, num2 = input("Enter two numbers: ").split()
num1 = int(num1)
num2 = int(num2)
sum_val = num1 + num2
diff = num1 - num2
product = num1 * num2
quotient = round(num1 / num2, 2)
print(f"Sum={sum_val}, Diff={diff}, Product={product}, Quotient={quotient}")
